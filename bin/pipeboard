#!/usr/bin/env node

require = require('esm')(module /*, options*/);
let path = require('path');
let platform = process.platform;
require(path.join(__dirname, '../src/' + platform + '/cli/cli.js')).cli(process.argv);