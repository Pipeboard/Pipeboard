version: 2.1
jobs:
  run-test:
    docker:
      - image: ubuntu
    steps:
      - prep-test
      - execute-test
commands:
  prep-test:
    steps:
      - run: apt-get install npm
      - run: apt-get install nodejs
      - run: npm install
  execute-test:
    steps:
      - run: npm run circleci_test
workflows:
   version: 2
   test:
     jobs:
       - run-test