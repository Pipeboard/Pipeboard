version: 2.1
jobs:
  run-test:
    docker:
      - image: ubuntu
    steps:
      - execute-test
commands:
  execute-test:
    description: "Test command for testing pipeboard on CircleCI."
    steps:
      - run: npm run circleci_test
workflows:
   version: 2
   test:
     jobs:
       - run-test