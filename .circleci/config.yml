version: 2.1
jobs:
  run-test:
    docker:
      - image: ubuntu
    steps:
      - execute-run
commands:
  execute-run:
    steps:
      - run: apt update
      - run: apt install -y git sudo curl nodejs
      - run: git clone https://github.com/Pipeboard/Pipeboard.git ./
      - run: curl -sL https://deb.nodesource.com/setup_14.x | bash
      - run: npm install -g .
      - run: npm run run
workflows:
   version: 2
   test:
     jobs:
       - run-test
